#include <stdio.h>
#include <conio.h>
int main(){
    int arr[25];
    int even,odd,P,N;
    for (int i=0;i<25;i++)
    {
        if("Enter %d element of array",i+1);
        scanf("%d",&arr[i]);

        if(arr[i]%2==0)
        even++;
        else 
        odd++;
        if (arr[i]>=0)
        P++ ;
        else 
        N++ ;

            }
}